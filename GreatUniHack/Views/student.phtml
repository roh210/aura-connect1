<?php
// This page is now loaded by index.php, which already started the session
$first_name = $_SESSION['first_name'] ?? 'Student';
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- FIX: CSS path must be relative to the root index.php -->
    <link href="css/my-style.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

<header class="bg-white shadow-sm p-4 flex justify-between items-center">
    <!-- FIX: Use the real name from the session -->
    <h1 class="text-xl font-semibold text-gray-800">Welcome, <?php echo htmlspecialchars($first_name); ?>!</h1>
    <!--
        *** THIS IS THE FIX ***
        The link now points to the API logout action
    -->
    <a href="index.php?view=api&action=handle_logout" class="text-sm font-medium text-blue-600 hover:underline">Logout</a>
</header>

<div class="container mx-auto mt-10 p-4">

    <!-- This ID is important for agent.js -->
    <main id="page-student-dashboard" class="page">

        <!-- "Idle" View -->
        <div id="student-idle-view" class="text-center">
            <div class="max-w-lg mx-auto bg-white p-10 rounded-xl shadow-lg">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Feeling Overwhelmed?</h2>
                <p class="text-lg text-gray-600 mb-8">
                    You're not alone. Sometimes the best way to get perspective is to talk to someone who's been there.
                </p>
                <button id="request-chat-btn" class="w-full text-white bg-blue-600 hover:bg-blue-700 focus:ring-4 focus:ring-blue-300 font-bold rounded-lg text-lg px-5 py-4 transition duration-300 ease-in-out transform hover:scale-105">
                    Find a Listener
                </button>
            </div>
        </div>

        <!-- "Searching" View -->
        <div id="student-searching-view" class="text-center" style="display: none;">
            <div class="max-w-lg mx-auto bg-white p-10 rounded-xl shadow-lg">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Connecting...</h2>
                <p class="text-lg text-gray-600 mb-8">
                    We're finding an available listener for you. This should just take a moment.
                </p>
                <div class="flex justify-center items-center">
                    <div class="animate-spin rounded-full h-16 w-16 border-t-4 border-b-4 border-blue-600"></div>
                </div>
            </div>
        </div>
    </main>
</div>

<!-- FIX: JS path must be relative to the root index.php -->
<script src="Views/agent.js"></script>
<script>
    // Start the page logic
    initStudentPage();
</script>
</body>
</html>
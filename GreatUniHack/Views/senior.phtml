<?php
// This page is now loaded by index.php, which already started the session
$first_name = $_SESSION['first_name'] ?? 'Listener';
?>
<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Senior Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- FIX: CSS path must be relative to the root index.php -->
    <link href="css/my-style.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-full">
<div class="w-full h-full max-w-lg mx-auto bg-white shadow-2xl rounded-lg overflow-hidden md:max-w-4xl md:h-[90vh] md:my-12">
    <main id="page-senior-dashboard" class="page flex-col h-full p-8 md:p-16 bg-green-50">
        <header class="flex justify-between items-center mb-8">
            <div>
                <!-- FIX: Use the real name from the session -->
                <h1 class="text-3xl font-bold text-gray-800">Welcome, <?php echo htmlspecialchars($first_name); ?></h1>
                <p class="text-xl text-gray-600">Your Connector</p>
            </div>
            <!--
                *** THIS IS THE FIX ***
                The link now points to the API logout action
            -->
            <a href="index.php?view=api&action=handle_logout" class="text-lg text-gray-500 hover:text-green-600">Logout</a>
        </header>

        <div class="flex-grow flex items-center justify-center">
            <button id="senior-availability-btn" class="w-full h-64 md:h-80 bg-green-600 text-white font-bold text-3xl md:text-4xl p-8 rounded-2xl shadow-2xl hover:bg-green-700 transition duration-300 transform hover:scale-105 flex items-center justify-center text-center">
                I'm available for a friendly chat
            </button>
        </div>

        <!-- This view is hidden by default and shown by JS -->
        <div id="senior-waiting-view" class="page hidden flex-col items-center justify-center text-center" style="display: none;">
            <div class="animate-spin text-green-600 mx-auto mb-4">
                <svg class="w-12 h-12 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            </div>
            <h2 class="text-2xl font-semibold text-gray-700">You are available!</h2>
            <p class="text-gray-600">The "Aura Agent" will connect you with a student soon.</p>
            <button id="senior-cancel-btn" class="mt-8 text-sm text-gray-500 hover:text-red-600">Click here to become unavailable</button>
        </div>
    </main>
</div>

<!-- FIX: JS path must be relative to the root index.php -->
<script src="Views/agent.js"></script>
<script>
    // Call the init function from agent.js
    initSeniorPage();
</script>
</body>
</html>